import { useState, useEffect, useMemo } from 'react';

export const makeState = useState;
export const makeEffect = useEffect;
export const makeMemo = useMemo;

export const onMount = () => {};
export const onUnmount = () => {};
